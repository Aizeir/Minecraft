#pragma once
#include "util.cpp"

const uint MAPW = 16;
const uint MAPH = 16;
const uint MAPD = 16;

class World {public:
    World() {
        int map[MAPW][MAPH][MAPD];

        for (uint x=0;x<MAPW;x++) {
            
        }
    }
};